node_modules
dist
.vercel

# Design reference material pulled in while building the redesign. It is
# working input, not part of the product: the system it describes lives
# transcribed in src/index.css, which is the authoritative copy.
theme.css
tokens.json
design/

# Throwaway Playwright capture scripts used while authoring screenshots.
# They hardcode absolute paths to other projects' node_modules and are
# regenerated per session, not committed.
shot-*.mjs
