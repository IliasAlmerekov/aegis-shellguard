{
  "comment": [
    "The release target matrix, and the only place a runner label is written.",
    "`.github/actions/load-versions` emits `targets` as the `build_targets` job",
    "output, which both `ci.yml` (cross-matrix) and `release.yml` (build) feed",
    "into `strategy.matrix.include`. It also derives the macOS runner labels",
    "for the jobs that name a runner outside the matrix, so a runner-image bump",
    "is a one-line change here.",
    "The macOS runners are pinned rather than `macos-latest` (which migrated to",
    "macOS 26 on 2026-06-15, see actions/runner-images#14167) so an OS bump",
    "never silently changes what a release is built on.",
    "`asset_name` is the published GitHub Release asset filename; the installer,",
    "the npm wrapper, and the Homebrew formula all resolve it, so renaming one",
    "breaks every install channel."
  ],
  "targets": [
    {
      "target": "x86_64-unknown-linux-musl",
      "os": "ubuntu-latest",
      "asset_name": "aegis-linux-x86_64",
      "use_cross": true
    },
    {
      "target": "aarch64-unknown-linux-musl",
      "os": "ubuntu-latest",
      "asset_name": "aegis-linux-aarch64",
      "use_cross": true
    },
    {
      "target": "x86_64-apple-darwin",
      "os": "macos-26-intel",
      "asset_name": "aegis-macos-x86_64",
      "use_cross": false
    },
    {
      "target": "aarch64-apple-darwin",
      "os": "macos-26",
      "asset_name": "aegis-macos-aarch64",
      "use_cross": false
    }
  ]
}
