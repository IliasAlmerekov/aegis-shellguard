[package]
name = "aegis-snapshot"
version = "0.6.3"
edition = "2024"
rust-version = "1.89"
license = "MIT"
repository = "https://github.com/IliasAlmerekov/aegis-shellguard"
homepage = "https://github.com/IliasAlmerekov/aegis-shellguard"
documentation = "https://docs.rs/aegis-snapshot"
description = "Snapshot and rollback plugins for Aegis."

[dependencies]
aegis-types = { version = "0.6.3", path = "../aegis-types" }
aegis-config = { version = "0.6.3", path = "../aegis-config" }
async-trait = "0.1"
thiserror = "1"
tokio = { version = "1", features = ["process", "fs", "rt", "rt-multi-thread", "io-util", "sync", "time"] }
tracing = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
time = { version = "0.3", features = ["formatting", "parsing"] }
# `std` exposes stored uid metadata but no effective-uid query; libc provides
# `geteuid` without adding a native build dependency.
libc = "0.2"

[dev-dependencies]
tempfile = "3"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
